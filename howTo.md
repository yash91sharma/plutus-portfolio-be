## Compine app for production

run `npm run build`, which will compile the app in `dist` directory.

This assumes that typescript is installed globally. If not, then run:
```
sudo npm i -g typescript
```

## Run the prod app

```
node dist/server.js
```

That's it folks!